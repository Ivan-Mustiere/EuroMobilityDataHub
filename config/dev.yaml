# Environnement DEV — développement et debug du pipeline, itération rapide
env: dev

database:
  path: environments/dev/db_dev.duckdb

# Échantillon réduit pour aller vite (voir claude.md, section Environnements)
sample:
  enabled: true
  # Une seule liaison TGV et quelques mois pour limiter le volume en dev
  liaisons_max: 1
  mois_max: 3

logging:
  level: DEBUG
