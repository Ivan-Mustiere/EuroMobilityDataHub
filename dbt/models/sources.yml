version: 2

sources:
  - name: staging
    description: >
      Couche Silver (Bloc 1, Tableau 5) : tables STAGING chargées par apps/pipeline/load_cloud.py via
      COPY INTO avec inférence de schéma — colonnes brutes, noms français d'origine conservés.
    schema: STAGING
    tables:
      - name: regularite_tgv
        identifier: REGULARITE_TGV
      - name: regularite_ter
        identifier: REGULARITE_TER
      - name: regularite_intercites
        identifier: REGULARITE_INTERCITES
      - name: gares
        identifier: GARES
      - name: dim_stations_multipays
        identifier: DIM_STATIONS_MULTIPAYS
