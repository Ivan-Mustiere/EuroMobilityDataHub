# Valeurs factices pour tester la stack en local (docker compose --profile local-test up) sans
# RDS réel — pointe vers le service postgres-test défini dans docker-compose.yml. Aucun secret ici.
PGHOST=postgres-test
PGPORT=5432
PGDATABASE=euromobilitydatahub
PGUSER=etl_admin
PGPASSWORD=localtest
