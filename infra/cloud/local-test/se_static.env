# Clé Trafiklab "GTFS Sweden 3 Static data" (palier Bronze, 60 requêtes/30j), lue par
# apps/pipeline/ingest.py pour télécharger le référentiel statique national suédois
# (build_se_reference, apps/pipeline/transform.py). Consommée dans le conteneur `airflow`
# (infra/cloud/docker-compose.yml) qui exécute le pipeline batch — différente de FEED_URL/`key`
# dans se.env (produit API distinct, cf. commentaire de se.env).
SE_GTFS_STATIC_KEY=2f2ce46e666746cdb40ffae82001e088
